server:
  port: 49030
spring:
  application:
    name: turbine-server
eureka:
  instance:
    prefer-ip-address: true
    instance-id: ${spring.cloud.client.ip-address}:${server.port}
  client:
    serviceUrl:
      defaultZone: http://localhost:49000/eureka/
logging:
  level:
    root: WARN